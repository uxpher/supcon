# 机械臂 + 灵巧手竞赛 · 技术方案汇总

> 生成日期：本次会话
> 适用范围：FTArm B9 + DexHand FDH12 + Gemini335 RGB-D
> 结论性质：已收敛，不再讨论「选哪个 VLA」

---

## 1. 硬件与任务约束

| 项目 | 情况 |
| --- | --- |
| 机械臂 | FTArm B9（Cartesian 控制，位置单位米，姿态 roll/pitch/yaw） |
| 灵巧手 | DexHand FDH12（12 自由度，UDP 通信，仅 Linux） |
| 相机 | Gemini335（结构光 RGB-D，**始终安装在机械臂末端 = eye-in-hand**） |
| GPU | 16 GB VRAM |
| 算法接口 | 自定义 HTTP 服务（/task1、/task2、/task3） |
| 赛前调试 | 允许真实设备标定和测试 |

### 三个任务

| 任务 | 内容 | 关键约束 |
| --- | --- | --- |
| Task 1 | 面板 3 盏灯，每盏灯下方有对应按钮或拨动开关；随机亮一盏未亮过的灯，机械臂对亮灯下方对应开关进行**点按或拨动**，共执行 3 次 | 按钮 → 点按；拨动开关 → 拨动 |
| Task 2 | 长方体顶面数字 1/2/3/4 | 4 个物体、全部竖直、数字仅顶面、放置槽位固定 |
| Task 3 | 三棱柱 / 六棱柱 / 长方体 / 圆柱 | 全部竖直、放置槽位固定 |

> Task 1 语义据官方赛题 PDF 归纳；最终以 PDF 原文为准。

---

## 2. 最终技术路线结论

> **不部署 π0-FAST-DROID / OpenVLA / TurboVLA / GR00T / Octo 作为主控模型。**

这是一个 **封闭场景 + 固定物体集合 + 固定工作空间 + 可提前标定 + RGB-D + 末端相机** 的问题，与 VLA 擅长的「大规模跨场景泛化、自然语言、未知物体」不匹配。

**主方案（确定性 pipeline）：**

```text
Gemini335 RGB-D
      │
      ▼
任务专用感知（Task1 亮灯/开关 · Task2 数字 · Task3 几何体）
      │
      ▼
目标 3D 定位（质心 + yaw + 置信度）
      │
      ▼
手眼标定：Camera → EEF → Robot World
      │
      ▼
FTArm B9  move_end_effector()
      │
      ▼
DexHand FDH12  预定义抓取手型
      │
      ▼
触觉 / 位置 / 电流 抓取验证（含失败重试）
```

第一版明确**砍掉**：VLM、SAM2、所有 VLA。感知失败优先怀疑光照/标定/深度/相机姿态/坐标变换，而不是「模型不够大」。

---

## 3. DexHand FDH12 接口摘要

### 3.1 设备与通信

- 通信协议：UDP；仅支持 Linux
- 默认 IP：左手 `192.168.137.19`，右手 `192.168.137.39`
- 型号：`FDH6`（`fdhv1`，6 自由度）/ `FDH12`（`fdhv2`，12 自由度）
- **注意：SDK 只有 6 和 12 自由度，没有「10 自由度」档**

### 3.2 12 自由度 → 手指/关节映射

| 列表索引（0-based） | 手指·关节 | `set_pos` 量程（原始编码） | `set_pvc` 弧度范围 |
| --- | --- | --- | --- |
| 0 | 食指 关节1 | 0–1750 | 0.20 ~ 1.69 |
| 1 | 食指 关节2 | 0–1780 | 0.01 ~ 1.43 |
| 2 | 食指 关节3 | 0–576 | -0.04 ~ 0.26 |
| 3 | 中指 关节1 | 0–1750 | 0.20 ~ 1.69 |
| 4 | 中指 关节2 | 0–1780 | 0.01 ~ 1.43 |
| 5 | 无名指 关节1 | 0–1750 | 0.20 ~ 1.69 |
| 6 | 无名指 关节2 | 0–1780 | 0.01 ~ 1.43 |
| 7 | 小指 关节1 | 0–1750 | 0.20 ~ 1.69 |
| 8 | 小指 关节2 | 0–1780 | 0.01 ~ 1.43 |
| 9 | 拇指 关节1 | 0–1700 | -0.02 ~ 1.23 |
| 10 | 拇指 关节2 | 0–1700 | 0.14 ~ 1.35 |
| 11 | 拇指 关节3 | 0–1700 | 0.20 ~ 1.57 |

> 文档正文用 1 基编号（「食指1-3」「拇指10-12」），但 `set_pos(pos)` 传 Python 列表是 0 基，即「食指1」= `pos[0]`。

### 3.3 关键方法

| 方法 | 用途 |
| --- | --- |
| `init()` / `get_ip_list()` | 初始化 / 扫描设备 |
| `set_pos(ip, pos)` / `get_pos(ip)` | 位置控制 / 读取（12 维，某位置不控制写 -1） |
| `set_pvc(ip, pvc)` / `get_pvc(ip)` | 弧度/速度/电流控制与读取 |
| `set_ctrl_mode(ip, mode)` | 控制模式（位置 / PD / 位置速度电流） |
| `get_ts_matrix(ip)` | **触觉传感器**（仅 FDH12，6×96 字节） |
| `get_errorcode(ip)` | 错误码 |

> ⚠️ SDK 文档红字警告：**「请不要使用过大的电流值，会造成手的严重损伤」** —— 抓取必须限流。

---

## 4. 关键工程决策（最终版）

### 4.1 手眼标定（第一优先级）

相机在末端，需求解固定外参 `T_eef_camera`，最终：

```
T_world_camera = T_world_eef · T_eef_camera
P_world = T_world_camera · P_camera
```

做法：

1. 桌面固定 Charuco/Aruco 标定板（不动）；
2. 标定相机 RGB 内参；
3. 机械臂采 **15~25 个空间分布良好、旋转变化充分的位姿**：不同 XYZ + 不同 Roll/Pitch/Yaw，**避免所有姿态近似平行**（否则 AX=XB 退化、条件极差）；
4. `cv2.calibrateHandEye()` 解 AX=XB；
5. **验证**：同一已知点在多个观察位下反投影到世界坐标，应基本重合；再让机械臂实际移动到 `P_world + safe_offset` 核对。

> ⚠️ 必须核对 OpenCV `calibrateHandEye()` 返回矩阵的方向（cam2gripper vs gripper2cam），不要只看变量名。

**评价指标：不要只看重投影误差，要看「最终任务空间 TCP 定位误差」。** 在抓取工作空间内取多个测试点，相机检测 → 转到 Base → 机械臂移动到点上方 → 实测偏差，记录 `mean / max / std`（目标如：mean < 3mm、max < 5mm）。因为总误差 = 相机 + 内参 + 手眼 + 机器人 + 深度 + 机械，最终要标定的是整条 **Camera → Base → TCP** 链。

### 4.2 深度处理（修正版）

- **单像素深度不可信**（边缘、反光、深色、近距、结构光最小工作距离处都会崩）；
- **对目标 ROI 取鲁棒深度**：Mask/ROI → 去边缘 → 剔除无效深度 → 中值/截尾均值，或用 **RANSAC 拟合顶面平面**（Task3 推荐：顶面分割 → 深度点云 → 平面拟合 → 平面中心）；
- **Z（脆弱轴）**：用「顶面平面拟合 + 已知物体高度 + 标定偏置」给预抓取点；下降段**全程不再依赖深度**，最后停点由位置/触觉/电流收尾；
- XY 对中等深度误差相对不敏感（放大因子 `(|u-cx|/fx)` 小），但**不要写成「XY 一定稳定」**——它取决于相机视角、物体在画面位置、深度与标定误差，应统一走鲁棒深度反投影。

> 特别提醒：Gemini335 是结构光，有最小工作距离。相机在末端，越下压越接近/低于该下限，正是最需要深度的那一下。所以「预抓取点用深度（距离还够）→ 之后按固定行程/接触反馈下降」。

### 4.3 平面内旋转角 yaw

> ⚠️ **2D 图像里的 yaw ≠ 机器人 Base 帧的 yaw**（相机不一定完全垂直桌面）。

正确做法：

```text
图像轮廓 → 恢复顶面方向向量（3D，Camera Frame）
   → 经 R_world_camera 转到 Base Frame
   → 投影到桌面 XY 平面
   → atan2(dy, dx) 得到真正的 Base-frame yaw
```

**更简单方案（推荐）**：赛前把观察姿态固定为「相机尽量垂直桌面」，此时 `yaw_base ≈ yaw_image + 固定角度偏置`，工程上最省事。

yaw 语义：

| 物体 | yaw 处理 |
| --- | --- |
| 圆柱 | 无意义（None） |
| 长方体 | `minAreaRect`/PCA 主方向，yaw ∈ [0, π) |
| 三棱柱 | 由三个顶点方向确定 |
| 六棱柱 | 旋转对称，yaw 只需 `mod π/3` |

> **先测抓取容差再决定精度**：FDH12 包络抓取对 yaw 可能不敏感（±10°~20° 也能抓稳）。Task3 第一版用「分类 + 3D 中心 + **粗 yaw**」，实测抓取后再决定是否投入高精度 yaw。

感知统一输出：

```python
ObjectInfo(type, position_xyz, yaw, confidence)
```

### 4.4 观察位 / 动作位分离（推广到全部任务）

相机在末端 = 相机正对物体时手也常悬在物体上方。所以**所有需要视觉定位的任务**统一走：

```text
OBSERVE → BUILD_TARGET_MAP → MOVE_SAFE → PRE_ACTION → ACTION
```

- Task1：观察位识别亮灯+开关 → 安全位 → 点按/拨动
- Task2：观察位识别数字 → 安全位 → 逐个抓取放置
- Task3：观察位几何识别 → 安全位 → 抓取

需赛前实测：**一个观察位能否覆盖全部目标**；不能则做 2~3 个固定观察位 + 多视角融合。并确认数字是印刷（RGB 即可）还是刻印/压印（需深度或侧光）。

### 4.5 抓取验证 + 限流保护（稳定态检测，不用固定延时）

`CLOSE_HAND` 后**用稳定态检测**，不要 `sleep(0.3)` 后单次判断：

```python
send_close_command()
while not timeout:
    pos = get_pos()
    pvc = get_pvc()
    tactile = get_ts_matrix()
    if grasp_stable_for_N_frames():   # 连续 N 帧：触觉有接触 + 位置变化很小
        break
```

判定：

| 情况 | 判断依据 |
| --- | --- |
| 夹空 | 手指几乎完全闭合 + 电流正常 + 触觉≈0 |
| 夹住 | 手指未达完全闭合 + 部分关节电流上升 + 触觉有接触 |

> 具体帧数 N、电流阈值、触觉阈值需按 FDH12 控制模式、手指速度、物体尺寸**在实物上调**，不作为固定常量写死。

同时**限流保护**：电流超阈值立即停，防止夹空硬夹或夹异物伤手。

### 4.6 失败恢复（必须加）

比赛中最常见的是「第一次抓空」。状态机需支持：

```text
VERIFY
  ├── SUCCESS → LIFT → PLACE
  └── FAIL → OPEN → RETREAT_SAFE → RE-OBSERVE → RETRY
```

设 `MAX_RETRY = 1~2`（前提：比赛规则允许重试，按最终规则执行）。否则抓空后继续 Lift→Place 会把整次机会浪费掉。

---

## 5. 分阶段计划（最终顺序）

| 阶段 | 内容 | 目标/验收 |
| --- | --- | --- |
| **Phase 0** | 设备连通：B9 + FDH12 + Gemini335 各自独立跑通 | 三个最小 demo 稳定 |
| **Phase 1** | 机械系统基线：B9 重复定位、FDH12 开合、触觉/电流读取 | 先知道硬件本身误差 |
| **Phase 2** | 手眼标定：内参 + AX=XB（旋转充分采样）+ 多姿态验证 + 任务空间误差测试 | mean<3mm / max<5mm（目标） |
| **Phase 3** | 写死坐标 Pick&Place（不接视觉） | 隔离控制 vs 视觉问题 |
| **Phase 4** | 抓取验证 + 失败恢复（触觉/位置/电流 + retry） | 抓空可自动重试 |
| **Phase 5** | Task3 感知：几何分类 + 3D 中心 + 粗 yaw | 第一个视觉验证任务 |
| **Phase 6** | Task2 感知：观察位覆盖 + 数字分类 + 多视角融合 | 数字稳定识别 |
| **Phase 7** | Task1：亮灯检测 + 面板标定 + 按钮点按/拨动开关轨迹 | 按赛题 PDF 语义 |

**Phase 3 的两个 KPI 分开测：**

- **抓取成功率**：同一点连续抓取 20 次 ≥19 成功；
- **重复定位误差**：重复移动到固定 TCP pose，测实际落点散布（<3mm）。

> 抓取失败 ≠ 定位误差（可能是手型/下降高度/闭合速度/滑动），拆开才方便 Debug。

---

## 6. 系统架构

```text
                    Competition Software
                            │
                            ▼
                    HTTP API Server
                    /task1  /task2  /task3
                            │
             ┌──────────────┴──────────────┐
             ▼                             ▼
       Perception                      Robot FSM
             │                             │
     ┌───────┼────────┐                    │
     ▼       ▼        ▼                    │
   Task1   Task2    Task3                  │
   亮灯    数字      几何                  │
     │       │        │                    │
     └───────┼────────┘                    │
             ▼                             │
        Object Map                         │
    (type, xyz, yaw, conf)                 │
             │                             │
             └──────────┬──────────────────┘
                        ▼
                 PickAndPlace()
                        │
        ┌───────────────┼───────────────┐
        ▼               ▼               ▼
   PRE_GRASP        DESCEND          GRASP
                                        │
                                        ▼
                              VERIFY (触觉/位置/电流)
                                   │
                    ┌──────────────┴──────────────┐
                    ▼                             ▼
                 SUCCESS                        FAIL
                    │                             │
                    ▼                          OPEN
                   LIFT                          │
                    │                        RETREAT_SAFE
                    ▼                             │
                   PLACE                    RE-OBSERVE
                                               │
                                               ▼
                                             RETRY
```

核心数据结构保持简单：

```python
@dataclass
class ObjectInfo:
    type: str          # "hexagonal_prism" / "block_1" / "switch_3" ...
    position: np.ndarray   # [x, y, z] in B9 base frame
    yaw: float | None
    confidence: float
```

控制层不感知「这是 Task2 还是 Task3」，只知道：抓什么、在哪里、朝哪个方向、放哪里。

---

## 7. 下一步行动

已确认推进顺序，下一步产出**可运行骨架代码**：

1. **Phase 0 设备闭环**：B9 `move_end_effector()` + FDH12 `set_pos/get_pvc/get_ts_matrix` + Gemini335 采 RGB-D，三个最小 demo；
2. **Phase 1 机械基线**：B9 重复定位、FDH12 开合与反馈读取；
3. **Phase 2 手眼标定脚本**：内参 + AX=XB（旋转充分采样）+ 任务空间误差验证（含 OpenCV 返回方向核对）；
4. **Phase 3/4 `pick_and_place()` 状态机**（含限流 + 稳定态验证 + 失败重试）；
5. **Phase 5 Task3 感知**（顶面轮廓 → `ObjectInfo(type, xyz, yaw, confidence)`，含 Camera→Base 的 yaw 变换）。
