必须现场调试确认、无法仅靠现有文档确定的事项：

  - B9 与 O10 的实际 IP、当前使用右臂还是左臂，以及对应的 mode/目标键。
  - O10 的实际开合方向；open_pose、point_pose 是否可用，食指按压时其他手指是否干涉。
  - ~~B9 控制末端参考点与真实食指尖的偏移（TCP）~~ —— 已绕开，不再必要：示教时摆「执行手型」再记位姿，使「记录姿态 = 执行姿态」，TCP 偏移自动隐含，无需单独标定。前提：每个动作位姿必须在对应手型下示教（换手型就重示教）。
  - 面板在机器人基座坐标系中的实际位置，以及安全位、观察位、两个按钮的接近/按压位、拨杆接近/起止位。
  - 每个灯与其下方开关的实际映射；按钮和拨杆在面板上的实际布局。
  - 拨杆的真实拨动方向、有效行程、接触姿态；按钮的有效行程和最小停留时间。
  - 上述所有位姿是否能直线规划、是否落在无碰撞空间；B9 是否出现 OMPL 回退。
  - 相机彩色流真实像素格式、分辨率、是否镜像/旋转，以及观察位是否完整覆盖三盏灯。
  - 赛场光照、反光和自动曝光下：三盏灯熄灭/点亮时的 ROI 亮度与颜色阈值、点亮稳定延时。
  - 操作完成后实际可观测的成功信号：对应灯是否熄灭或发生何种状态变化。
  - 灵巧手、相机、机械臂实际响应延迟；据此设置拍照等待、按压停留、动作超时。
  - 现场实体急停位置、异常时允许的处置方式，以及工控机上算法服务的真实启动/联网权限。

---

## 附：逐条查证结论（能否从官方文档 / 官网 SDK 找到答案）

> 查证范围：赛事官方文档（决赛赛题 PDF、算法对接 docx、B9/O10 HTTP API 文档）+ 官网 SDK（AGILINK OmniHand SDK、Orbbec Gemini335）。
> 一句话：接口层静态量（端口/命名/工作域/超时）文档可答；现场物理量（IP/位置/光照/延迟）任何文档都答不了，只能现场确认。

| # | 待确认事项 | 官方文档 | 官网 SDK | 最终结论 |
| --- | --- | --- | --- | --- |
| 1 | 实际 IP、左右臂、mode/目标键 | ⚠️ 端口 8087/8088、`right_arm/left_arm`、`right/left` 有；实际 IP、用哪只臂无 | ❌ | 命名规则有，IP/左右臂现场定 |
| 2 | O10 开合方向、手型、食指干涉 | ❌ 文档 §3.1 与 §4.6 自相矛盾 | ⚠️ SDK 有 API 但未写清开合语义 | 必须实物实测 |
| 3 | 末端参考点与食指尖 TCP 偏移 | ❌ 无（灵巧手无几何尺寸） | ❌ 产品页无毫米级手指长度 | 现场示教（摆 point_pose 记位姿） |
| 4 | 面板位置、各段位姿 | ❌ 无 | ❌ | 现场示教 |
| 5 | 灯↔开关映射、布局 | ⚠️ 只给「3灯+2按钮+1拨动」 | ❌ | 具体映射/布局现场看实物 |
| 6 | 拨动方向/行程、按钮行程/停留 | ❌ 无 | ❌ | 现场看实物 |
| 7 | 位姿能否直线、OMPL 回退 | ✅ 工作域 Y∈[-0.28,-0.04]、Z∈[0.44,0.52] + OMPL 判定 | — | 规则有，每个位姿现场 verify |
| 8 | 相机像素格式/分辨率/镜像/覆盖 | ❌ 官方明说 Gemini335 不在本地文档内 | ✅ Orbbec 官网有 datasheet | 规格能查，镜像/覆盖现场确认 |
| 9 | 赛场光照阈值、稳定延时 | ❌ 无 | ❌ | 现场调 |
| 10 | 操作后成功信号（灯是否变化） | ❌ PDF 只说完成操作，没说灯会变 | ❌ | 现场确认 |
| 11 | 实际响应延迟 | ⚠️ 只给接口超时/推送间隔（60s、20ms/50ms） | ❌ | 接口值有，实际延迟现场测 |
| 12 | 急停位置、联网权限 | ⚠️ 只给安全纪律 | ❌ | 现场确认 |

### 关键补充结论（深挖 SDK 后）

1. **型号**：机械臂、灵巧手为**赛事专供版本，不披露具体型号**，安装位姿详见官方示意图。网上 SDK 参数（速度限制、左右手镜像范围等）只作背景，不能当赛事设备的确定值。
2. **触觉**：O10 硬件有 1D 触觉（400+ 点），但赛事仅提供 HTTP API 且**未暴露触觉接口** → 实际按「无触觉」处理（电流 + 堵转 + 位置）。
3. **尺寸**：SDK 文档层（README / API / 运动学头文件）均无毫米级手指长度 / 关节间距，尺寸不公开。现场「安装位姿示意图 + 现场量」是唯一来源；实际抓取靠示教 + 接触反馈，不依赖手工测量。
4. **接口**：现场单台电脑有线网络接入；只能走赛事官方文档给的 HTTP API（B9 :8087、O10 :8088），SDK 的触觉 / 手势 / 被动关节能力用不了。