# 并行与底层优化方案

> 配套文档：`技术方案.md`（决赛版技术方案）
> 前提：单臂 + 单灵巧手 + 单相机（eye-in-hand）。**"真并行"的物理空间很小**——一条臂、一个末端相机，抓取动作只能串行。因此本文的"并行"绝大多数是**设备间重叠 + 流水线 + 后台监控**，而不是"同时做两件抓取"。

---

## 0. 结论先行

| 类别 | 建议 | 优先级 |
| --- | --- | --- |
| 底层优化 | 臂/手 **REST 换 WebSocket**、速度分段、预标定+相对偏移 | 高（零风险、收益直接） |
| 并行① | 机械臂运动 ⊥ 灵巧手动作（两条独立连接重叠） | 高（最大收益） |
| 并行② | 主控流程 ⊥ 后台安全监控协程 | **必须做**（安全兜底） |
| 并行③ | 任务空档 ⊥ 健康检查/预热 | 中 |
| 伪并行 | 边运动边拍照 / 任务间并行 / 多客户端控硬件 | **禁止** |

---

## 一、底层优化（单步更快更稳）

### 1.1 通信层（收益直接，零风险）

| 优化 | 做法 | 收益 |
| --- | --- | --- |
| 臂走 **WebSocket** | `end_effector` 用 WS 异步，替代 REST 阻塞 | 无 60s 上限 + 有 `feedback` 进度，长距离不超时 |
| 手走 **WebSocket** | `set_pos/set_pvc` 用 WS 异步指令 + 订阅 50ms `status` 流 | 抓取验证闭环不再 REST 轮询，省每 50ms 一次 HTTP 往返 |
| 连接复用 | 单条 WS 长连接，别每次重建握手 | 省 TCP 三次握手延迟 |
| 超时对齐 | `end_effector≥90s`、`playback≥310s` | 避免误判超时 |

### 1.2 运动层

- **速度分段**：长距离转运 `velocity_scaling=0.25~0.3`；下压/入槽降到 `0.05~0.08`。高速用在安全空域，低速用在贴近目标的危险段。
- **直线更密**：`cartesian_eef_step` 从 0.025 降到 0.01，插值更细，末端轨迹更接近真直线。
- **先预览后执行**：每个目标先 `plan_only:true` 试可达性，失败立刻换点，不浪费时间撞「OMPL 回退」。
- **固定轨迹用 playback**：Task1 的「拨动开关轨迹」、固定「观察位↔安全位」往返，赛前示教录成 `.csv` 回放（`speed_scale` 可加速），比每次在线规划快且可复现。

### 1.3 感知层

- **预标定 + 相对偏移**：槽位/面板/台面的绝对坐标赛前示教存表，运行时视觉**只输出「相对标定位置的偏移量」**，机械臂 = 存表坐标 + 偏移。误差小、快、稳，精度和速度双赢。
- **ROI + 降分辨率**：只处理目标 ROI，图像降到识别所需最低分辨率；数字/形状用模板匹配 + 轮廓特征。
- **离线做重活**：手型表、观察位、标定偏置、电流/堵转阈值全部赛前标定好，比赛时零调参。

### 1.4 抓取验证层

- 用 WS 的 `status` 流做**高频闭环**（手 50ms 一帧、臂 20ms 一帧），替代 REST 轮询。
- 闭合指令用 WS 异步发出后**立即开始读状态**，边闭合边判堵转，不用等 `set_pos` 阻塞返回（REST 版 `set_pos` 阻塞到完成，WS 版不阻塞）。

---

## 二、Pipeline 并行化（三个实在的点）

### ② 主控流程 ⊥ 后台安全监控协程（必须并行）

独立一个后台协程，**持续订阅臂和手的 WS status 流**，与主控制流程并行，任何异常直接触发急停，主流程无需轮询：

```python
import asyncio, json, websockets

class SafetyMonitor:
    def __init__(self, arm_ws, hand_ws):
        self.arm_ws, self.hand_ws = arm_ws, hand_ws
        self.emergency = asyncio.Event()

    async def run(self):
        """全程后台运行：手过流 + 臂 fault 监控"""
        async def watch_hand():
            async for raw in self.hand_ws:
                m = json.loads(raw)
                if m.get("type") != "status":
                    continue
                d = m["data"]
                if any(c & 0b100 for c in d.get("error_codes", [])):   # 过流 bit2
                    print("[SAFETY] hand over_current, emergency stop")
                    self.emergency.set()

        async def watch_arm():
            async for raw in self.arm_ws:
                m = json.loads(raw)
                if m.get("type") == "status":
                    # 可在此检查 arm 侧软标记；电机 fault 仍走 /api/motors 低频轮询
                    pass

        await asyncio.gather(watch_hand(), watch_arm())

    def is_emergency(self):
        return self.emergency.is_set()
```

> 臂的电机级 `fault/feedback_age` 在 `/api/motors`（REST），不在 status 流里，另起一个低频轮询协程（如每 500ms）即可，同样并行。

### ① 机械臂运动 ⊥ 灵巧手动作（最大收益）

臂和手是**两个独立 HTTP 服务、两条独立连接**，天然可以重叠：

```
串行版：臂到位 → 手开手 → 臂下降 → 手闭合 → 臂提升 → 手开手 → 臂转运...
并行版：
  臂：  观察位 → 安全位 → 预抓取位 → 下降 → 抓取 → 提升 → 放置位 → 下降 → 放置
  手：        开手(预抓取手型)         闭合               开手(并行于转运)
```

**关键：臂在移动的这段时间，手提前摆好下一个手型**（开手/预张到目标宽度）。一个协程管臂、一个协程管手：

```python
async def grasp_cycle(arm, hand, obj):
    """臂手重叠的抓取流程骨架"""
    # 臂去预抓取位 与 手预张 并行
    await asyncio.gather(
        arm.line_to(obj.x, obj.y, obj.z_safe),     # 臂异步直线运动
        hand.set_pos(OPEN_POSE)                     # 手提前开手
    )
    # 下降 + 闭合（此段必须顺序：到位才能闭）
    await arm.line_to(obj.x, obj.y, obj.z_grasp)
    await hand.set_pvc(CLOSE_PVC, torque=[200]*10)
    # 验证期间臂已可并行回安全位？不——先验证成功再提，见 §三 伪并行
    verdict = await hand.verify_grasp()
    if verdict == "GRASPED":
        await asyncio.gather(
            arm.line_to(obj.x, obj.y, obj.z_safe),  # 提升
            # 手保持闭合不动（无需动作）
        )
```

### ③ 任务空闲 ⊥ 健康检查/预热

竞赛软件调 `/api/taskN/execute` 之间有空档（尤其 Task1 三次调用之间）。空闲时后台做：

- `GET /api/motors` 健康检查（`fault==0`、`has_feedback==1`、`feedback_age<0.1`）；
- 手复位到 `OPEN_POSE`；
- 相机预热曝光 / 白平衡稳定。

让每次任务起跑时状态就绪，省掉任务内的准备耗时。

---

## 三、不要做的"伪并行"（会翻车）

1. **边运动边拍照** —— 相机在末端，运动时画面糊、深度更崩。拍照必须静止在观察位。
2. **任务间并行** —— 三个任务共享同一套硬件，同时跑会触发 B9 的**抢占语义**（后发目标抢占旧目标），必然互踩。任务接口只能串行。
3. **多客户端并发控臂/手** —— 服务器不做业务互斥，多连接后发先至，危险。保持"你的算法服务 = 唯一控制方"。
4. **为并行上重框架** —— 竞赛场景稳定优先，别为并行引入多进程/消息队列。**asyncio 单进程 + 两个 WS 连接 + 一个监控协程**就够，足以覆盖上述全部并行。

---

## 附：推荐技术选型

| 项 | 选择 | 理由 |
| --- | --- | --- |
| 并发模型 | `asyncio` 单进程 | 足够覆盖臂/手双 WS + 监控协程，无进程间通信复杂度 |
| 臂通信 | `websockets`（`ws://<IP>:8087/ws`） | 异步无 60s 上限，`feedback/result` 推进度 |
| 手通信 | `websockets`（`ws://<IP>:8088/ws`） | 异步指令 + 50ms `status` 流做闭环 |
| 监控 | 独立 `asyncio.Task` | 过流/堵转/臂 fault 即时触发急停 |
| 任务编排 | 单状态机（串行）+ 协程内 `gather` 局部重叠 | 稳定优先，不把单臂硬拗成并行 |

> 一句话：底层优化重点在「WS 换 REST 阻塞 + 速度分段 + 预标定偏移」；并行只有三处实在的——**臂/手设备间重叠、后台安全监控、空闲预热**。控制层本身仍是串行状态机。
