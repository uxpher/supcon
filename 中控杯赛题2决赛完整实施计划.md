# “中控杯”赛题 2 决赛完整实施计划

> 版本：v1.0  
> 适用赛题：工业多模态感知与无人化智能操作（外操无人化）  
> 决赛日期：2026 年 8 月 19–20 日  
> 核心原则：**稳定完成优先于速度；安全优先于单次得分。**

## 1. 赛题结论与总策略

决赛由比赛软件通过 HTTP 调用队伍部署在赛台工控主机上的算法服务。软件仅根据接口调用的成功状态和耗时记录结果；视觉识别、深度定位、机械臂运动、灵巧手控制、抓取、放置和容错均由队伍完成。

推荐采用“**固定工装坐标标定 + RGB-D 视觉确认 + 单线程状态机**”方案，而不是依赖端到端大模型：赛题的槽位、台面与操作面板均为固定工装，可靠标定和动作复核更容易获得稳定高分。

```text
比赛操作软件 HTTP
        ↓
算法服务（健康检查、任务接口、状态机、日志）
        ↓
感知 → 置信度检查 → 轨迹规划 → 执行 → 结果复核
        ↓                    ↓
Gemini335 RGB-D      机械臂 HTTP / 灵巧手 HTTP
```

### 1.1 最新规则必须落实

- 任务 2 的长方体仅**顶面**有数字，识别时必须先调整到可见顶面的观察姿态。
- 任务 3 的几何体必须**全部竖直摆放**；放入槽位且主体完全位于工位边界内才可得满分。
- 每项赛题最多两次演示机会；机器人或灵巧手碰撞设备、道具变形、移位或损毁会导致该任务失败。
- 应先完成全部任务要求，再考虑缩短时间；不能用未确认的高速动作换取耗时优势。

## 2. 评分重点与任务拆解

| 任务 | 满分结构 | 关键得分动作 | 实施优先级 |
|---|---:|---|---:|
| 任务 1：按拨开关 | 完成度 90 + 设备保护 10 | 正确识别亮灯、完成对应按键或拨动，连续三次正确 | P2 |
| 任务 2：数字长方体有序放置 | 每件 22 × 4 = 88 + 安全 12 | 严格按 1→2→3→4；抓起、放到指定台面、保持原槽姿态 | P1 |
| 任务 3：几何体无序分拣 | 每件 22 × 4 = 88 + 安全 12 | 形状识别、正确入同名槽、竖直且完全入位 | P1 |

### 2.1 任务 2 的单件得分构成

| 判分项 | 分值 | 系统验收点 |
|---|---:|---|
| 有效抓取与转运 | 4 | 物块抓起并离开原槽 |
| 抓取顺序 | 6 | 必须是 1→2→3→4 |
| 放置指定台面 | 8 | 物块成功位于指定台面 |
| 放置姿态 | 4 | 放置姿态与原放置槽内姿态一致 |

### 2.2 任务 3 的单件得分构成

| 判分项 | 分值 | 系统验收点 |
|---|---:|---|
| 有效抓取与转运 | 6 | 抓起、无掉落并离开初始区域 |
| 准确放置 | 12 | 放入外侧形状名称一致的槽位 |
| 入位完成 | 4 | 主体完全在槽位边界内，竖直且不悬在工位外 |

## 3. 软件架构

### 3.1 对外接口

必须实现以下最小接口，并始终返回 JSON：

| 接口 | 方法 | 行为 |
|---|---|---|
| `/api/health` | `GET` | 返回服务、相机、机械臂、灵巧手是否就绪 |
| `/api/task1/execute` | `POST` | 完成一次亮灯后对应的按拨动作 |
| `/api/task2/execute` | `POST` | 完成长方体识别、按序抓取与放置 |
| `/api/task3/execute` | `POST` | 完成形状识别、抓取与对应槽位摆放 |

成功示例：

```json
{"success": true, "message": "task2 completed"}
```

失败示例：

```json
{"success": false, "message": "target 3 confidence too low; returned to safe pose"}
```

### 3.2 建议模块划分

```text
app/
├── server.py                 # HTTP 服务和接口路由
├── orchestrator.py           # 单线程任务状态机、超时与恢复
├── config.yaml               # 标定坐标、速度、手势、阈值
├── perception/
│   ├── camera.py             # Gemini335 数据采集、帧同步
│   ├── digit.py              # 顶面数字 1–4 识别
│   ├── geometry.py           # 形状分类与目标位姿估计
│   └── verification.py       # 抓取/放置后的视觉复核
├── devices/
│   ├── arm_client.py         # 机械臂 HTTP 客户端
│   └── hand_client.py        # 灵巧手 HTTP 客户端
├── skills/
│   ├── pick_place.py         # 预抓、抓取、抬升、放置、撤离
│   └── panel_operation.py    # 按键、拨动开关动作
└── logs/                     # 时间戳、图像、检测结果、设备响应
```

### 3.3 状态机原则

所有设备控制必须由一个任务线程串行发出，禁止并发运动请求。每个关键步骤均应有超时、检查和安全退回分支。

```text
INIT → SELF_CHECK → SAFE_POSE → OBSERVE → DETECT
  → CONFIRM → PRE_GRASP → GRASP → LIFT_CONFIRM
  → PRE_PLACE → PLACE → PLACE_CONFIRM → NEXT_TARGET
  → SAFE_POSE → SUCCESS

任一步失败 → SAFE_RETREAT → FAILURE
```

只有最后的 `PLACE_CONFIRM` 全部通过，任务接口才返回 `success: true`。

## 4. 标定与坐标体系

### 4.1 必做标定

1. Gemini335 内参与 RGB-D 对齐检查。
2. 眼在手手眼标定：求得相机坐标系到末端、再到机械臂基座的变换。
3. 固定工装标定：初始槽位、放置台、形状槽位、操作面板和安全观察位。
4. 灵巧手姿态标定：张手、半握、窄捏、宽捏、按压、拨动六种姿态。
5. 动作高度标定：观察、预抓、抓取、抬升、预放、放置、撤离高度。

### 4.2 `config.yaml` 建议内容

```yaml
poses:
  safe: {x: 0.275, y: -0.16, z: 0.50, roll: -3.141, pitch: -1.552, yaw: 3.141}
  observe_rack: {}
  observe_panel: {}
  task2_table: {}
  task3_slots: {cube: {}, cylinder: {}, prism: {}, polyhedron: {}}
motion:
  transit_velocity: 0.20
  contact_velocity: 0.08
  pre_grasp_height: 0.05
  lift_height: 0.06
  place_height: 0.01
grasp:
  narrow_pinch: []
  wide_pinch: []
  press: []
thresholds:
  digit_confidence: 0.92
  shape_confidence: 0.90
  placement_tolerance_m: 0.012
```

所有数值应由现场实测填写；代码中禁止散落硬编码坐标。

## 5. 视觉方案

### 5.1 双阶段感知

每个目标采用两次感知，避免单帧深度噪声直接决定抓取：

1. **全局观察**：在固定高位连续采集 5 帧 RGB-D，取中值深度，识别全部目标和候选区域。
2. **局部复核**：移动至目标上方预抓位再次拍摄，计算抓取中心、顶面法向、深度有效性与周边障碍。

推荐 Gemini335 在全局抓取识别时使用较高深度填充率预设；接近和精放时使用高精度预设或多帧深度融合。近距离眼在手任务应优先验证相机近距深度模式。

### 5.2 任务 2：顶面数字与物块位姿

1. 在安全观察位拍摄图像，使用货架区域 ROI 限制检测范围。
2. 基于深度平面和 RGB 边缘分割四个长方体候选。
3. 对每个候选的**顶面**做透视矫正，再通过模板匹配或轻量 OCR 识别 1–4。
4. 将识别结果映射为 `{数字 → 抓取位姿}`；任一数字缺失、重复或置信度不足时换观察角度重拍。
5. 依照 `1 → 2 → 3 → 4` 锁定任务队列，队列创建后不可按空间位置重排。

### 5.3 任务 3：几何体识别

采用 RGB 轮廓与深度几何特征融合，而不是仅靠颜色：

- 轮廓：圆度、角点数量、长宽比、凸包特征；
- 深度：顶面面积、侧壁斜率、平面数、高度；
- 分类：规则化的模板匹配/轻量分类器；
- 置信度不足：改变相机观察角度后重试，禁止猜测放槽。

每个类别在 `config.yaml` 中有唯一目标槽位。执行前先检查该槽未被占用，执行后用视觉确认目标主体完全进入槽内。

### 5.4 任务 1：亮灯识别

- 固定观察位拍 RGB 图，采用 HSV 阈值或颜色聚类识别发亮指示灯。
- 由“灯位 → 下方执行器类型/坐标”的标定表决定按键还是拨动开关动作。
- 动作后复拍确认目标灯状态变化；若状态未变化，退回安全位并记录失败原因。

## 6. 操作技能设计

### 6.1 通用抓取与放置

```text
张手 → 预抓位 → 局部复拍 → 垂直下降 → 合手
→ 低速抬升 → 视觉确认离槽 → 预放位 → 垂直下降
→ 张手 → 视觉确认放稳/入槽 → 垂直撤离
```

约束：

- 横向移动只在预抓/预放安全高度进行。
- 下降、接触、释放阶段低速；空载横移可中速。
- 直线动作优先使用笛卡尔直线规划，首条轨迹可先仅规划验证。
- 规划失败、深度无效、抓取后未抬起或放置后偏位时，立即退回安全位。

### 6.2 任务 2 专项

- 选择能保持物块竖直的对称捏持点，避免抓取后翻转。
- 放置位的偏差预算应小于台面边界安全余量；以视觉复核后的物块中心，而非理论轨迹末端作为判定依据。
- 每完成一件，重新拍摄剩余区域，防止抓取扰动导致后续目标坐标失效。

### 6.3 任务 3 专项

- 一律竖直抓取、竖直下降、竖直释放。
- 先放置底部较大、较稳定物体；但不得以此破坏对应槽位或任务要求。
- 圆柱和多面体使用较低下降速度，释放后观察是否倾斜、悬边或压线。

### 6.4 任务 1 专项

- 按钮：食指伸出，沿面板法向低速按压、短暂停留、原路径退出。
- 拨动开关：先接触，再按标定的切向轨迹缓慢拨动，禁止横向冲击面板。
- 三次调用均应是独立任务，不可假定上次亮灯或机械臂位置仍然有效。

## 7. 设备控制与安全

### 7.1 机械臂

- 运动接口为同步阻塞式，客户端超时必须高于服务端动作时长。
- 新运动命令会抢占旧动作，因此业务层只保留一个控制方。
- 直线工作时应保持在赛方推荐的安全工作域；若响应信息表明回退为自由规划，应视为告警并停止该次动作链。
- 建议速度：接触阶段 0.08–0.15；空载转场 0.15–0.25；仅在连续稳定后再上调。

### 7.2 灵巧手

- 使用归一化位置控制接口，不直接使用电机极限位置。
- 现场记录各物体的最终可靠抓手姿态，而不是凭理论关节角直接使用。
- 每次抓取前检查设备连接和错误码；错误码非零时不执行任务。

### 7.3 安全规则

- 任意异常必须先撤离，再返回失败；禁止在目标区域盲目补抓。
- 机械臂、相机、槽位、操作台均不得碰撞。
- 每个任务前从安全位开始；每个任务结束回安全位。
- 日志必须保存原始图、检测结果、设备响应、任务状态和耗时，便于现场定位问题。

## 8. 测试与验收

### 8.1 自动化测试

- HTTP：健康检查、JSON 返回、重复调用、异常请求与超时。
- 设备：断相机、机械臂不可达、灵巧手错误码、深度无效。
- 视觉：暗光、反光、目标偏移、数字模糊、形状遮挡。
- 状态机：每一步失败均验证是否安全退回并输出可读错误。

### 8.2 上场验收门槛

| 项目 | 上场最低要求 |
|---|---|
| 任务 2 | 连续 20 次全流程成功率 ≥ 90%，顺序零错误，无碰撞 |
| 任务 3 | 连续 20 次分类正确率 ≥ 95%，完全入槽率 ≥ 90% |
| 任务 1 | 连续 30 次亮灯定位与动作正确率 ≥ 95% |
| 服务接口 | 四个接口、异常返回、重复调用和离线运行均通过 |
| 部署 | 工控主机离线可运行，模型/依赖/配置/U 盘备份齐全 |

## 9. 倒排计划

| 日期 | 交付物 | 当日目标 |
|---|---|---|
| 8 月 14 日 | 服务骨架、接口联调、日志模块 | 使用赛方模拟器完成 HTTP 通讯 |
| 8 月 15 日 | 相机采集、数字识别、形状分类 | 形成离线视觉数据集和阈值初值 |
| 8 月 16 日 | 手眼标定、工装坐标、手势表 | 单物体抓取与放置稳定完成 |
| 8 月 17 日 | 任务 2、任务 3 全流程 | 开始 20 次连续成功率统计 |
| 8 月 18 日 | 任务 1、故障注入、部署包 | 冻结代码、依赖与配置，制作 U 盘备份 |
| 8 月 19–20 日 | 现场标定与演示 | 仅调整配置参数，不临场修改核心逻辑 |

## 10. 现场操作清单

### 入场后

- [ ] 检查工控主机、网络地址、Python 环境与离线依赖。
- [ ] 启动算法服务，验证 `/api/health`。
- [ ] 检查 Gemini335 RGB、深度、时间戳和近距离深度效果。
- [ ] 检查机械臂状态、末端位姿、灵巧手状态和错误码。
- [ ] 执行安全位、观察位、预抓位的低速空载验证。
- [ ] 复核各槽位、台面、面板与相机的标定偏差。
- [ ] 从 U 盘恢复最终版本，保存现场配置副本。

### 演示中

- [ ] 在规定时间内点击“开始”。
- [ ] 每次任务调用前确认服务在线。
- [ ] 观察日志与实时画面，但不向机器人发出额外人工控制命令。
- [ ] 先保证完整度和安全得分，后考虑耗时。
- [ ] 确认全部任务达标后再提交，避免误提交造成不可逆后果。

## 11. 风险与处置

| 风险 | 预防 | 处置 |
|---|---|---|
| 顶面数字看不清 | 固定高位观察 + 透视矫正 + 多帧融合 | 改观察角度重拍；仍低置信度则安全失败 |
| 深度噪声或空洞 | 选择合适预设、5 帧中值、局部复拍 | 使用 RGB 轮廓约束，禁止盲抓 |
| 抓取滑落 | 现场标定抓手姿态、抬升确认 | 退回安全位，不在原地补抓 |
| 放置偏位 | 预放高度、低速垂降、放后复核 | 撤离并判失败，保留第二次演示机会 |
| 运动规划回退/超时 | 先规划、保持安全域、限制速度 | 取消后退回安全位，记录响应信息 |
| 相机或设备异常 | 健康检查与预检 | 立即告知工作人员，按赛方流程处理 |

## 12. 最终决策原则

1. 先拿完整度和安全分，再追求速度。
2. 低置信度不猜测；未知目标不抓取。
3. 任何异常都统一回安全位。
4. 现场只修改标定配置和阈值，不临时重构程序。
5. 将第二次演示机会视为恢复机会，而不是试错机会。
